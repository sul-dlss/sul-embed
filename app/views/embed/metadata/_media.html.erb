<%= render partial: 'metadata/media_accessibility_note',
      layout: 'metadata_panel',
      locals: {
        viewer: viewer,
        panel: Embed::MetadataPanel.new(viewer.purl_object)
      } %>
