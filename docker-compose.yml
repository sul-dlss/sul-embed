# docker-compose.yml

version: '3.2'

services:
  webpacker:
    build: .
    ports:
    - '3035:3035'
    environment:
      RAILS_ENV: development
      NODE_ENV: development
      WEBPACKER_DEV_SERVER_HOST: 0.0.0.0
    volumes:
      - .:/usr/src/app